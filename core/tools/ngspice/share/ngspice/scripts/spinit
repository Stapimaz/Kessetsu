* Standard ngspice init file
alias exit quit
alias acct rusage all
set x11lineararcs
*set rndseed=12
** ascii rawfile **
set filetype=ascii
** frontend debug output **
*set ngdebug
** asking after quit **
*set askquit
** set the number of threads in openmp
** default (if compiled with --enable-openmp) is: 2
set num_threads=8
*set interactive

* Kessetsu ships the minimal analog runtime profile. Optional XSPICE code
* models and OpenVAF/OSDI models are not part of this bundle; do not try to
* load files that are intentionally absent. Add a separate runtime profile
* before enabling either model family.
unset osdi_enabled
